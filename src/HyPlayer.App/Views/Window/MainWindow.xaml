<ex:WindowEx
    x:Class="HyPlayer.App.Views.Window.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="using:WinUIEx"
    xmlns:local="using:HyPlayer.App.Views.Window"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:HyPlayer.App.Views.Controls.App"
    xmlns:search="using:HyPlayer.App.Views.Controls.Search"
    Title="HyPlayer"
    MinWidth="500"
    MinHeight="300"
    SystemBackdrop="{ThemeResource DefaultMicaSystemBackdrop}"
    mc:Ignorable="d">

    <ex:WindowEx.WindowContent>
        <Grid>
            <TeachingTip
                x:Name="globalTeachingTip"
                Title="一个弹窗, 两个弹窗, 三个弹窗, 四个弹窗...zzzzzz"
                IsLightDismissEnabled="False"
                PreferredPlacement="TopRight" />
            <Border
                x:Name="AppTitleBar"
                Height="48"
                Margin="48,0,0,0"
                VerticalAlignment="Top"
                Canvas.ZIndex="1">
                <Grid x:Name="TitleBarContainer" Height="48">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="LeftPaddingColumn" Width="0" />
                        <ColumnDefinition x:Name="IconColumn" Width="Auto" />
                        <ColumnDefinition x:Name="TitleColumn" Width="Auto" />
                        <ColumnDefinition x:Name="LeftDragColumn" Width="*" />
                        <ColumnDefinition x:Name="SearchColumn" Width="Auto" />
                        <ColumnDefinition x:Name="RightDragColumn" Width="*" />
                        <ColumnDefinition x:Name="SettingColumn" Width="Auto" />
                        <!--<ColumnDefinition x:Name="PersonPictureButton" Width="Auto" />-->
                        <ColumnDefinition x:Name="RightPaddingColumn" Width="0" />
                    </Grid.ColumnDefinitions>
                    <Image
                        x:Name="TitleBarIcon"
                        Grid.Column="1"
                        Width="16"
                        Height="16"
                        Margin="8,0,0,0"
                        Source="ms-appx:///Assets/Images/StoreLogo.png" />
                    <TextBlock
                        x:Name="TitleTextBlock"
                        Grid.Column="2"
                        Margin="6,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind Title}" />
                    <search:SearchBox x:Name="SearchBox"
                                      Grid.Column="4"
                                      Width="400"
                                      Height="36"
                                      MinWidth="250"
                                      MaxWidth="550"
                                      VerticalAlignment="Center"
                                      BorderThickness="0"/>
                    <Button
                        Grid.Column="5"
                        x:Name="UserButton"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Style="{ThemeResource TitleBarButtonStyle}"
                        Click="UserButton_Click"
                        Height="40"
                        Width="40"
                        CornerRadius="20">
                        <PersonPicture Width="30" Height="30"/>
                    </Button>

                    <Button
                        x:Name="SettingButton"
                        Grid.Column="6"
                        VerticalAlignment="Center"
                        Style="{ThemeResource TitleBarButtonStyle}"
                        Height="40"
                        Width="40"
                        CornerRadius="20">
                            <FontIcon Width="30" Height="30" Glyph="&#xE713;" FontSize="16"/>
                        </Button>

                </Grid>
            </Border>

            <Frame x:Name="rootFrame" Margin="0,0,0,0" Canvas.ZIndex="0"/>

            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState>
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="641" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="AppTitleBar.Background" Value="{ThemeResource AcrylicBackgroundFillColorBaseBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
                <VisualStateGroup>
                    <VisualState>
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="1008" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="AppTitleBar.Background" Value="{ThemeResource AcrylicBackgroundFillColorBaseBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

        </Grid>
    </ex:WindowEx.WindowContent>
</ex:WindowEx>

<?xml version="1.0" encoding="utf-8"?>
<local:UserPageBase
    x:Class="HyPlayer.Views.Pages.UserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HyPlayer.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid x:Name="GridHeader">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <PersonPicture HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" ProfilePicture="{x:Bind ViewModel.NcmUser, Converter={StaticResource CoverToImageSourceConverter}, ConverterParameter=256}"/>
            <TextBlock Grid.Column="1" HorizontalAlignment="Left" Style="{ThemeResource TitleTextBlockStyle}" Text="{x:Bind ViewModel.UserName}" />
        </Grid>
        

    </Grid>
</local:UserPageBase>
